/**
 * `user_providers.provider_name` for email + password (local) accounts.
 * Must match values used by the auth module for local registration and login.
 */
export const USER_PROVIDER_NAME_LOCAL = 'local' as const;
