/** Default auto-dismiss duration for toasts (ms). */
export const TOAST_DEFAULT_DURATION_MS = 4000;

/** Suppress a new toast if the same variant + title + message was shown within this window (ms). */
export const TOAST_DEDUPE_WINDOW_MS = 3500;

/** Accessible label for the toast list region. */
export const TOAST_REGION_ARIA_LABEL = "Alert notifications";

/** Accessible label for the dismiss control on each toast. */
export const TOAST_DISMISS_ARIA_LABEL = "Dismiss notification";
